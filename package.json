{
  "name": "vela-exchange-beta",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ vela-exchange-beta",
    "create-local": "graph create --node http://localhost:8020/ vela-exchange-beta",
    "remove-local": "graph remove --node http://localhost:8020/ vela-exchange-beta",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 vela-exchange-beta",
    "test": "graph test"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.55.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
